{
  "name": "inmobiliaria-mkalpin",
  "version": "1.0.0",
  "description": "Sistema Inmobiliario MKalpin - Full Stack",
  "scripts": {
    "build": "cd Frontend && npm install && npm run build",
    "dev": "cd Frontend && npm start",
    "start": "cd Frontend && npm start",
    "vercel-build": "cd Frontend && npm install && npm run build"
  },
  "dependencies": {
    "mongodb": "^6.18.0",
    "jsonwebtoken": "^9.0.2",
    "bcryptjs": "^3.0.2",
    "cloudinary": "^2.5.0"
  },
  "devDependencies": {}
}
