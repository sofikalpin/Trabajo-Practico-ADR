# Configuración de MongoDB
MONGODB_URI=mongodb+srv://<usuario>:<contraseña>@cluster0.nesg8rs.mongodb.net/inmobiliaria?retryWrites=true&w=majority

# Configuración de JWT
JWT_SECRET=tu_clave_secreta_muy_segura_aqui
JWT_EXPIRES_IN=30d

# Configuración del servidor
PORT=3001
NODE_ENV=development

# Configuración de CORS (opcional, separar por comas sin espacios)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Configuración de Cloudinary (si lo estás usando)
CLOUDINARY_CLOUD_NAME=tu_cloud_name
CLOUDINARY_API_KEY=tu_api_key
CLOUDINARY_API_SECRET=tu_api_secret

# Configuración del frontend (URL base para las redirecciones)
FRONTEND_URL=http://localhost:3000

# Configuración de correo (si es necesario)
EMAIL_HOST=smtp.ejemplo.com
EMAIL_PORT=587
EMAIL_USER=tu_correo@ejemplo.com
EMAIL_PASSWORD=tu_contraseña
EMAIL_FROM=noreply@tudominio.com
